services:
  web:
    image: huseyinurak/parsingapp-web:amd64_tag
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
      - ./db.sqlite3:/app/db.sqlite3
    ports:
      - "8000:8000"